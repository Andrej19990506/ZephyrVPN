# План тестирования проверки остатков при создании заказа

## ✅ Что уже готово:
1. ✅ Миграция 018 применена (добавлены `nomenclature_id`, `recipe_id`)
2. ✅ Миграция 019 применена (добавлен `portion_weight_grams`)
3. ✅ Код проверки остатков интегрирован в `CreateOrder`

---

## 🧪 Сценарии тестирования:

### 1. Тест: Создание заказа с достаточными остатками

**Подготовка:**
```sql
-- Создаем тестовый филиал
INSERT INTO branches (id, name) VALUES ('test-branch-1', 'Тестовый филиал 1') ON CONFLICT DO NOTHING;

-- Создаем номенклатуру для теста
INSERT INTO nomenclature_items (id, sku, name, base_unit, is_saleable, is_ready_for_sale)
VALUES 
  ('test-nom-1', 'TEST-FLOUR', 'Мука тестовая', 'kg', true, true),
  ('test-nom-2', 'TEST-CHEESE', 'Сыр тестовый', 'kg', true, true)
ON CONFLICT DO NOTHING;

-- Создаем партии товаров (достаточно для заказа)
INSERT INTO stock_batches (id, nomenclature_id, branch_id, quantity, unit, cost_per_unit, remaining_quantity)
VALUES 
  ('test-batch-1', 'test-nom-1', 'test-branch-1', 100.0, 'kg', 50.0, 100.0),
  ('test-batch-2', 'test-nom-2', 'test-branch-1', 50.0, 'kg', 200.0, 50.0)
ON CONFLICT DO NOTHING;

-- Создаем Recipe для тестовой пиццы
INSERT INTO recipes (id, name, is_active, portion_size, unit)
VALUES ('test-recipe-1', 'Тестовая пицца', true, 1.0, 'pcs')
ON CONFLICT DO NOTHING;

-- Создаем ингредиенты рецепта (нужно 1кг муки и 0.5кг сыра на 1 пиццу)
INSERT INTO recipe_ingredients (id, recipe_id, nomenclature_id, quantity, unit)
VALUES 
  ('test-ri-1', 'test-recipe-1', 'test-nom-1', 1000.0, 'g'),  -- 1кг муки
  ('test-ri-2', 'test-recipe-1', 'test-nom-2', 500.0, 'g')   -- 0.5кг сыра
ON CONFLICT DO NOTHING;
```

**Тест:**
```bash
POST http://localhost:8080/api/v1/orders
Content-Type: application/json

{
  "branch_id": "test-branch-1",
  "is_pickup": true,
  "items": [
    {
      "pizza_name": "Тестовая пицца",
      "quantity": 1,
      "extras": []
    }
  ]
}
```

**Ожидаемый результат:** ✅ Заказ создан успешно (HTTP 200)

---

### 2. Тест: Создание заказа с недостаточными остатками

**Подготовка:**
```sql
-- Уменьшаем остатки (оставляем только 0.5кг муки, а нужно 1кг)
UPDATE stock_batches 
SET remaining_quantity = 0.5 
WHERE id = 'test-batch-1';
```

**Тест:**
```bash
POST http://localhost:8080/api/v1/orders
Content-Type: application/json

{
  "branch_id": "test-branch-1",
  "is_pickup": true,
  "items": [
    {
      "pizza_name": "Тестовая пицца",
      "quantity": 1,
      "extras": []
    }
  ]
}
```

**Ожидаемый результат:** ❌ HTTP 400 с сообщением:
```json
{
  "error": "Недостаточно ингредиентов для выполнения заказа",
  "details": "пицца 'Тестовая пицца' (x1): недостаточно остатков для ингредиента 'Мука тестовая': требуется 1000.00 г, доступно 500.00 г"
}
```

---

### 3. Тест: Проверка допов с достаточными остатками

**Подготовка:**
```sql
-- Создаем доп "Доп. сыр"
INSERT INTO extras (id, name, price, nomenclature_id, portion_weight_grams, is_active)
VALUES (1, 'Доп. сыр', 100, 'test-nom-2', 50, true)
ON CONFLICT (name) DO UPDATE SET nomenclature_id = EXCLUDED.nomenclature_id;

-- Восстанавливаем остатки сыра
UPDATE stock_batches 
SET remaining_quantity = 10.0 
WHERE id = 'test-batch-2';
```

**Тест:**
```bash
POST http://localhost:8080/api/v1/orders
Content-Type: application/json

{
  "branch_id": "test-branch-1",
  "is_pickup": true,
  "items": [
    {
      "pizza_name": "Тестовая пицца",
      "quantity": 1,
      "extras": ["Доп. сыр"]
    }
  ]
}
```

**Ожидаемый результат:** ✅ Заказ создан успешно

---

### 4. Тест: Проверка допов с недостаточными остатками

**Подготовка:**
```sql
-- Уменьшаем остатки сыра (оставляем 0.01кг = 10г, а нужно 50г для допа)
UPDATE stock_batches 
SET remaining_quantity = 0.01 
WHERE id = 'test-batch-2';
```

**Тест:**
```bash
POST http://localhost:8080/api/v1/orders
Content-Type: application/json

{
  "branch_id": "test-branch-1",
  "is_pickup": true,
  "items": [
    {
      "pizza_name": "Тестовая пицца",
      "quantity": 1,
      "extras": ["Доп. сыр"]
    }
  ]
}
```

**Ожидаемый результат:** ❌ HTTP 400 с сообщением:
```json
{
  "error": "Недостаточно ингредиентов для выполнения заказа",
  "details": "доп 'Доп. сыр' для пиццы 'Тестовая пицца' (x1): недостаточно остатков для допа 'Доп. сыр': требуется 50.00 г, доступно 10.00 г"
}
```

---

### 5. Тест: Заказ без branch_id (проверка должна быть пропущена)

**Тест:**
```bash
POST http://localhost:8080/api/v1/orders
Content-Type: application/json

{
  "is_pickup": true,
  "items": [
    {
      "pizza_name": "Тестовая пицца",
      "quantity": 1,
      "extras": []
    }
  ]
}
```

**Ожидаемый результат:** ❌ HTTP 400 с сообщением:
```json
{
  "error": "Недостаточно ингредиентов для выполнения заказа",
  "details": "branch_id не указан, невозможно проверить остатки"
}
```

---

### 6. Тест: Заказ с пиццей без рецепта

**Подготовка:**
```sql
-- Создаем пиццу в меню, но без Recipe
-- (используем существующую пиццу из PizzaRecipe, но без Recipe)
```

**Тест:**
```bash
POST http://localhost:8080/api/v1/orders
Content-Type: application/json

{
  "branch_id": "test-branch-1",
  "is_pickup": true,
  "items": [
    {
      "pizza_name": "Пицца без рецепта",
      "quantity": 1,
      "extras": []
    }
  ]
}
```

**Ожидаемый результат:** ❌ HTTP 400 с сообщением:
```json
{
  "error": "Недостаточно ингредиентов для выполнения заказа",
  "details": "пицца 'Пицца без рецепта': рецепт не найден - рецепт не найден для пиццы 'Пицца без рецепта': не найден ни NomenclatureItem (IsSaleable=true), ни Recipe с таким именем. Пицца должна иметь связанный Recipe для проверки остатков"
}
```

---

## 🔧 Быстрый тест через curl:

```bash
# 1. Проверка достаточных остатков
curl -X POST http://localhost:8080/api/v1/orders \
  -H "Content-Type: application/json" \
  -d '{
    "branch_id": "test-branch-1",
    "is_pickup": true,
    "items": [{"pizza_name": "Тестовая пицца", "quantity": 1, "extras": []}]
  }'

# 2. Проверка недостаточных остатков
curl -X POST http://localhost:8080/api/v1/orders \
  -H "Content-Type: application/json" \
  -d '{
    "branch_id": "test-branch-1",
    "is_pickup": true,
    "items": [{"pizza_name": "Тестовая пицца", "quantity": 10, "extras": []}]
  }'
```

---

## 📊 Проверка логов:

После каждого теста проверяйте логи сервера:
```bash
# Должны быть сообщения:
✅ "Проверка остатков пройдена для всех позиций"
⚠️ "Недостаточно остатков для ингредиента..."
❌ "Рецепт не найден для пиццы..."
```

---

## 🎯 Что проверить:

1. ✅ Заказ создается, если остатков достаточно
2. ❌ Заказ НЕ создается, если остатков недостаточно
3. ✅ Понятные сообщения об ошибках с указанием конкретного ингредиента
4. ✅ Проверка работает для допов
5. ✅ Проверка работает для нескольких позиций в заказе
6. ✅ Проверка работает для количества > 1



