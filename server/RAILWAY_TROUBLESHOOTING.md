# Railway Troubleshooting Guide

## Проблема: PostgreSQL не подключается

Если вы видите ошибки типа:
```
⚠️ PostgreSQL connection failed
⚠️ Menu service not started: PostgreSQL not available
```

### Решение 1: Проверьте подключение PostgreSQL сервиса

1. В Railway Dashboard откройте ваш проект
2. Убедитесь, что PostgreSQL сервис добавлен:
   - Нажмите "+ New" → "Database" → "PostgreSQL"
3. Убедитесь, что сервисы связаны:
   - В настройках вашего Go сервиса должна быть переменная `DATABASE_URL`
   - Railway автоматически устанавливает эту переменную при подключении PostgreSQL

### Решение 2: Проверьте переменные окружения

1. Откройте ваш Go сервис в Railway Dashboard
2. Перейдите в "Variables"
3. Убедитесь, что `DATABASE_URL` присутствует
4. Формат должен быть: `postgresql://user:password@host:port/database`

### Решение 3: Проверьте логи

1. В Railway Dashboard откройте "Deployments"
2. Выберите последний деплой
3. Проверьте логи на наличие:
   - `📋 DATABASE_URL установлен: ...` - означает, что переменная есть
   - `🔄 Подключение к PostgreSQL: ...` - означает попытку подключения
   - `✅ PostgreSQL подключен успешно` - означает успешное подключение

### Решение 4: Ручная установка DATABASE_URL

Если Railway не установил `DATABASE_URL` автоматически:

1. Откройте PostgreSQL сервис в Railway Dashboard
2. Перейдите в "Connect" или "Variables"
3. Скопируйте значение `DATABASE_URL` или `POSTGRES_URL`
4. В настройках Go сервиса добавьте переменную:
   - Name: `DATABASE_URL`
   - Value: (скопированное значение)

### Решение 5: Проверьте формат URL

Railway может предоставлять URL в формате `postgresql://`, но код автоматически конвертирует его в `postgres://` для GORM.

Если проблема сохраняется, проверьте логи на наличие:
- `🔧 Нормализован DATABASE_URL: postgresql:// → postgres://`

### Решение 6: Проверьте SSL режим

Railway PostgreSQL использует SSL по умолчанию. Убедитесь, что в URL есть параметр:
- `?sslmode=require` или `?sslmode=verify-full`

Код автоматически обрабатывает это, но если проблема сохраняется, проверьте логи.

## Проблема: Redis не подключается

Аналогично PostgreSQL:

1. Добавьте Redis сервис: "+ New" → "Database" → "Redis"
2. Railway автоматически установит `REDIS_URL`
3. Проверьте переменные окружения

## Проблема: Порт не определен

Railway автоматически устанавливает переменную `PORT`. Сервер уже настроен для чтения этой переменной.

Если проблема сохраняется:
1. Проверьте логи на наличие: `🚀 Server starting on port ...`
2. Убедитесь, что сервер слушает на `0.0.0.0` (уже настроено)

## Проблема: Миграции не выполняются

Миграции выполняются автоматически при старте через `models.AutoMigrate()`.

Если миграции не выполняются:
1. Проверьте логи на наличие: `✅ Database migrations completed`
2. Если видите ошибки миграций, проверьте права доступа к базе данных
3. Выполните миграции вручную через Railway CLI или Dashboard

## Полезные команды Railway CLI

```bash
# Установите Railway CLI
npm i -g @railway/cli

# Войдите в Railway
railway login

# Подключитесь к проекту
railway link

# Просмотрите логи
railway logs

# Просмотрите переменные окружения
railway variables

# Подключитесь к PostgreSQL через CLI
railway connect postgres
```

## Контакты

Если проблема не решена:
1. Проверьте [Railway Documentation](https://docs.railway.app/)
2. Присоединитесь к [Railway Discord](https://discord.gg/railway)
3. Проверьте логи в Railway Dashboard

