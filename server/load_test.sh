#!/bin/bash

echo "🚀 Запуск нагрузочного тестирования Go сервера"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

# Проверяем что сервер запущен
if ! curl -s http://localhost:8080/api/v1/menu > /dev/null; then
    echo "❌ Ошибка: Сервер не отвечает на http://localhost:8080"
    echo "   Убедитесь что сервер запущен: docker-compose up"
    exit 1
fi

echo "✅ Сервер доступен"
echo ""

# Запускаем нагрузочный тест
go run load_test.go

