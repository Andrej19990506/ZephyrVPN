# Настройка Railway - Пошаговая инструкция

## Проблема: DATABASE_URL не установлен

Если в логах видите:
```
📋 DATABASE_URL установлен: postgres://***@localhost/zephyrvpn?sslmode=disable
❌ PostgreSQL connection failed: failed to connect to database: failed to connect to `host=localhost...
```

Это означает, что Railway **НЕ установил** переменную `DATABASE_URL` автоматически.

## Решение: Ручная настройка PostgreSQL

### Шаг 1: Добавьте PostgreSQL сервис

1. В Railway Dashboard откройте ваш проект
2. Нажмите **"+ New"** → **"Database"** → **"PostgreSQL"**
3. Railway создаст новый PostgreSQL сервис

### Шаг 2: Получите DATABASE_URL

После создания PostgreSQL сервиса:

1. Откройте PostgreSQL сервис в Railway Dashboard
2. Перейдите в раздел **"Variables"** или **"Connect"**
3. Найдите переменную **`DATABASE_URL`** или **`POSTGRES_URL`**
4. Скопируйте значение (оно будет в формате: `postgresql://user:password@host:port/database`)

### Шаг 3: Добавьте DATABASE_URL в Go сервис

1. Откройте ваш Go сервис в Railway Dashboard
2. Перейдите в **"Variables"**
3. Нажмите **"+ New Variable"**
4. Добавьте:
   - **Name:** `DATABASE_URL`
   - **Value:** (вставьте скопированное значение из PostgreSQL сервиса)
5. Нажмите **"Add"**

### Шаг 4: Свяжите сервисы (опционально, но рекомендуется)

1. В настройках Go сервиса найдите раздел **"Settings"**
2. В разделе **"Service Connections"** или **"Dependencies"**
3. Добавьте PostgreSQL сервис как зависимость
4. Railway автоматически установит переменные окружения

## Альтернативный способ: Использование Railway Service Variables

Railway может предоставлять переменные через Service Variables:

1. В PostgreSQL сервисе найдите **"Connect"** или **"Variables"**
2. Railway может предоставлять переменные:
   - `DATABASE_URL`
   - `POSTGRES_URL`
   - `PGDATABASE_URL`
   - `POSTGRES_HOST`
   - `POSTGRES_PORT`
   - `POSTGRES_USER`
   - `POSTGRES_PASSWORD`
   - `POSTGRES_DB`

3. Если Railway не устанавливает `DATABASE_URL` автоматически, соберите его вручную:
   ```
   postgresql://POSTGRES_USER:POSTGRES_PASSWORD@POSTGRES_HOST:POSTGRES_PORT/POSTGRES_DB?sslmode=require
   ```

## Настройка Redis (опционально)

Аналогично PostgreSQL:

1. Добавьте Redis сервис: **"+ New"** → **"Database"** → **"Redis"**
2. Скопируйте `REDIS_URL` из Redis сервиса
3. Добавьте `REDIS_URL` в переменные Go сервиса

## Проверка после настройки

После добавления `DATABASE_URL` и перезапуска сервиса, в логах должно появиться:

```
📋 DATABASE_URL установлен: postgres://***@railway-host:port/database
🔄 Подключение к PostgreSQL: postgres://***@railway-host:port/database
✅ PostgreSQL подключен успешно
✅ Database migrations completed
```

Если видите `host=localhost` - значит все еще используется значение по умолчанию!

## Troubleshooting

### DATABASE_URL все еще localhost

1. Проверьте, что переменная добавлена в **Go сервис**, а не в PostgreSQL сервис
2. Убедитесь, что имя переменной точно `DATABASE_URL` (без пробелов)
3. Перезапустите Go сервис после добавления переменной
4. Проверьте логи - должно быть `📋 DATABASE_URL установлен: postgres://***@railway-host...`

### Ошибка подключения к PostgreSQL

1. Проверьте, что PostgreSQL сервис запущен (зеленый индикатор)
2. Убедитесь, что в URL указан правильный хост (не localhost!)
3. Проверьте SSL режим - Railway требует `sslmode=require` или `sslmode=verify-full`

### Redis ошибка "too many colons"

Код теперь автоматически парсит Redis URL. Если проблема сохраняется:
1. Проверьте формат `REDIS_URL` - должен быть `redis://host:port` или `redis://host:port/db`
2. Убедитесь, что Redis сервис запущен

## Полезные ссылки

- [Railway PostgreSQL Documentation](https://docs.railway.app/databases/postgresql)
- [Railway Environment Variables](https://docs.railway.app/develop/variables)
- [Railway Service Connections](https://docs.railway.app/develop/service-connections)

